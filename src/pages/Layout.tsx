import { Hero } from "@/components/Hero/Hero";
import Timer from "@/components/Timer/Timer";
const Layout = () => {
  return (
    <main className="">
      <Hero />
      <Timer />
    </main>
  );
};

export default Layout;
